{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Comment",
  "description": "JSONPlaceholder Comment resource schema — validates PATCH /comments/{id}",
  "type": "object",
  "required": ["id"],
  "properties": {
    "id":     { "type": "integer", "minimum": 1 },
    "postId": { "type": "integer", "minimum": 1 },
    "name":   { "type": "string" },
    "email":  { "type": "string" },
    "body":   { "type": "string" }
  },
  "additionalProperties": false
}
