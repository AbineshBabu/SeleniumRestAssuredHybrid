name: API Tests
on:
  push:
    branches:
      - main

jobs:
  api-tests:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Check GoRest connectivity
        shell: pwsh
        run: |
          curl -i -H "Authorization: Bearer ${{ secrets.GOREST_TOKEN }}" https://gorest.co.in/public/v2/users

      - name: Run API Tests
        run: mvn clean test "-Dcucumber.filter.tags=@api"